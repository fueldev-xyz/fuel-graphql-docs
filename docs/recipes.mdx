---
title: Recipes
---

# Recipes

您可以在下面看到并测试示例查询和突变。
单击“运行”按钮以运行相应的查询并查看响应。
单击“TypeScript”、“Apollo Client”或“urql”按钮以查看代码示例。

- [获取地址的资产余额](#获取地址的资产余额)
- [列出地址的所有资产余额](#列出地址的所有资产余额)
- [列出地址的所有交易](#列出地址的所有交易)
- [列出最新交易](#列出最新交易)
- [获取合约的资产余额](#获取合约的资产余额)
- [列出合约的所有资产余额](#列出合约的所有资产余额)
- [列出最新区块](#列出最新区块)
- [按高度获取区块信息](#按高度获取区块信息)
- [列出地址拥有的所有消息](#列出地址拥有的所有消息)
- [模拟运行事务](#模拟运行事务)
- [提交事务](#提交事务)
- [更多示例](#更多示例)

## 获取地址的资产余额

<GQLExamples.Balance />

<CodeExamples
  file="../examples/tests/balance.test.ts"
  ts_testCase="使用 TypeScript 获取余额"
  apollo_testCase="使用 Apollo 获取余额"
  urql_testCase="使用 urql 获取余额"
/>

## 列出地址的所有资产余额

<GQLExamples.Balances />

<CodeExamples
  file="../examples/tests/balances.test.ts"
  ts_testCase="使用 TypeScript 获取所有余额"
  apollo_testCase="使用 Apollo 获取所有余额"
  urql_testCase="使用 urql 获取所有余额"
/>

## 列出地址的所有交易

<GQLExamples.Transactions />

<CodeExamples
  file="../examples/tests/transactions-by-owner.test.ts"
  ts_testCase="使用 TypeScript 获取交易"
  apollo_testCase="使用 Apollo 获取交易"
  urql_testCase="使用 urql 获取交易"
/>

## 列出最新交易

<GQLExamples.LatestTransactions />

<CodeExamples
  file="../examples/tests/latest-transactions.test.ts"
  ts_testCase="使用 TypeScript 获取最新交易"
  apollo_testCase="使用 Apollo 获取最新交易"
  urql_testCase="使用 urql 获取最新交易"
/>

## 获取合约的资产余额

<GQLExamples.ContractBalance />

<CodeExamples
  file="../examples/tests/contract-balance.test.ts"
  ts_testCase="使用 TypeScript 获取合约余额"
  apollo_testCase="使用 Apollo 获取合约余额"
  urql_testCase="使用 urql 获取合约余额"
/>

## 列出合约的所有资产余额

<GQLExamples.ContractBalances />

<CodeExamples
  file="../examples/tests/contract-balances.test.ts"
  ts_testCase="使用 TypeScript 获取所有合约余额"
  apollo_testCase="使用 Apollo 获取所有合约余额"
  urql_testCase="使用 urql 获取所有合约余额"
/>

## 列出最新区块

<GQLExamples.LatestBlocks />

<CodeExamples
  file="../examples/tests/latest-blocks.test.ts"
  ts_testCase="使用 TypeScript 获取最新区块"
  apollo_testCase="使用 Apollo 获取最新区块"
  urql_testCase="使用 urql 获取最新区块"
/>

## 按高度获取区块信息

<GQLExamples.BlockByHeight />

<CodeExamples
  file="../examples/tests/block.test.ts"
  ts_testCase="使用 TypeScript 按高度获取区块信息"
  apollo_testCase="使用 Apollo 按高度获取区块信息"
  urql_testCase="使用 urql 按高度获取区块信息"
/>

## 列出地址拥有的所有消息

<GQLExamples.MessageInfo />

<CodeExamples
  file="../examples/tests/messages.test.ts"
  ts_testCase="使用 TypeScript 获取消息"
  apollo_testCase="使用 Apollo 获取消息"
  urql_testCase="使用 urql 获取消息"
/>

## 模拟运行事务

```graphql
mutation 模拟运行($encodedTransaction: HexString!, $utxoValidation: Boolean) {
  dryRun(tx: $encodedTransaction, utxoValidation: $utxoValidation) {
    receiptType
    data
  }
}
